version: '3.8'

services:
  ai-log-tool:
    build: .
    ports:
      - "5000:5000"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
    volumes:
      - ~/.kube:/home/appuser/.kube:ro
    restart: unless-stopped
