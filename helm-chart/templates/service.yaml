apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-log-tool.fullname" . }}
  labels:
    {{- include "ai-log-tool.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "ai-log-tool.selectorLabels" . | nindent 4 }}
